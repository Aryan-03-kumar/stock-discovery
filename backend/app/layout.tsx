import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "stock-discovery — claude.ai skill for Indian-market research",
  description:
    "A research copilot for fundamental discovery on Indian stocks (NSE/BSE). Translates a sector thesis into companies, scans 10-12 years of financials for anomalies, answers cross-stock comparison questions, and learns from your accept/reject decisions over time.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable}`}>
      <body>{children}</body>
    </html>
  );
}
